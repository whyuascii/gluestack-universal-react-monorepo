/**
 * UI Package Types
 *
 * Shared types used across the UI package.
 * For config types (RBAC, constants), see @app/config.
 *
 * Note: InviteDetails is exported from screens/group/InviteAccept.tsx
 */

export type { ScreenProps, NavigationProps } from "./screens";
