/**
 * Public Layout - Marketing pages layout
 *
 * No navigation bar needed here - HomeScreen includes its own PublicNavbar.
 */

export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
