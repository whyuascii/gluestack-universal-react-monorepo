name: PR Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

# Cancel previous runs for the same PR
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  NODE_VERSION: 20

jobs:
  # Setup job to install dependencies once and cache for other jobs
  setup:
    name: Setup Dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Cache pnpm store
        uses: actions/cache@v5
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache node_modules
        uses: actions/cache@v5
        with:
          path: |
            node_modules
            apps/*/node_modules
            packages/*/node_modules
          key: ${{ runner.os }}-nodemodules-${{ hashFiles('**/pnpm-lock.yaml') }}

  # Quick validation job
  validate:
    name: Quick Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Check PR title follows conventional commits
        uses: amannn/action-semantic-pull-request@v6
        if: github.event_name == 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            FEAT
            FIX
            DOCS
            STYLE
            REFACTOR
            TEST
            CHORE
            PERF
            CI
            BUILD
          requireScope: false

      - name: Check for merge conflicts
        run: |
          git fetch origin ${{ github.base_ref }}
          if git merge-tree $(git merge-base HEAD origin/${{ github.base_ref }}) HEAD origin/${{ github.base_ref }} | grep -q '<<<<<<<'; then
            echo "Merge conflicts detected!"
            exit 1
          fi

  # Changed files detection for smart CI
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.filter.outputs.api }}
      web: ${{ steps.filter.outputs.web }}
      mobile: ${{ steps.filter.outputs.mobile }}
      packages: ${{ steps.filter.outputs.packages }}
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Check changed files
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            api:
              - 'apps/api/**'
              - 'packages/database/**'
              - 'packages/auth/**'
              - 'packages/core-contract/**'
            web:
              - 'apps/web/**'
              - 'packages/components/**'
              - 'packages/ui/**'
            mobile:
              - 'apps/mobile/**'
              - 'packages/components/**'
              - 'packages/ui/**'
            packages:
              - 'packages/**'
            docs:
              - 'docs/**'
              - '*.md'

  # Fast check that runs in parallel
  quick-check:
    name: Quick Check
    runs-on: ubuntu-latest
    needs: [setup, changes]
    if: needs.changes.outputs.packages == 'true' || needs.changes.outputs.api == 'true' || needs.changes.outputs.web == 'true' || needs.changes.outputs.mobile == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Restore node_modules cache
        uses: actions/cache@v5
        with:
          path: |
            node_modules
            apps/*/node_modules
            packages/*/node_modules
          key: ${{ runner.os }}-nodemodules-${{ hashFiles('**/pnpm-lock.yaml') }}

      - name: Run quick lint
        run: pnpm lint

      - name: Run type check
        run: pnpm typecheck

  # Size check for bundle impact
  size-check:
    name: Bundle Size Check
    runs-on: ubuntu-latest
    needs: [setup, changes]
    if: needs.changes.outputs.web == 'true' || needs.changes.outputs.mobile == 'true' || needs.changes.outputs.packages == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: Restore node_modules cache
        uses: actions/cache@v5
        with:
          path: |
            node_modules
            apps/*/node_modules
            packages/*/node_modules
          key: ${{ runner.os }}-nodemodules-${{ hashFiles('**/pnpm-lock.yaml') }}

      - name: Build web app
        if: needs.changes.outputs.web == 'true'
        run: pnpm --filter web build
        env:
          # Dummy values for build-time config (actual values loaded at runtime)
          NEXT_PUBLIC_REVENUECAT_API_KEY: "dummy-key-for-ci-build"
          NEXT_PUBLIC_REVENUECAT_ENTITLEMENT_PREMIUM: "premium"
          NEXT_PUBLIC_REVENUECAT_PRODUCT_MONTHLY: "monthly"
          NEXT_PUBLIC_REVENUECAT_PRODUCT_YEARLY: "yearly"
          NEXT_PUBLIC_REVENUECAT_DEFAULT_OFFERING: "default"

      - name: Build mobile app
        if: needs.changes.outputs.mobile == 'true'
        run: pnpm --filter mobile build
        env:
          # Dummy values for build-time config (actual values loaded at runtime)
          EXPO_PUBLIC_REVENUECAT_API_KEY: "dummy-key-for-ci-build"
          EXPO_PUBLIC_REVENUECAT_ENTITLEMENT_PREMIUM: "premium"
          EXPO_PUBLIC_REVENUECAT_PRODUCT_MONTHLY: "monthly"
          EXPO_PUBLIC_REVENUECAT_PRODUCT_YEARLY: "yearly"
          EXPO_PUBLIC_REVENUECAT_DEFAULT_OFFERING: "default"

      - name: Analyze bundle size
        run: |
          if [ -d "apps/web/.next" ]; then
            echo "### Web Bundle Size" >> $GITHUB_STEP_SUMMARY
            du -sh apps/web/.next | awk '{print "Total: " $1}' >> $GITHUB_STEP_SUMMARY
          fi
          if [ -d "apps/mobile/.expo" ]; then
            echo "### Mobile Bundle Size" >> $GITHUB_STEP_SUMMARY
            du -sh apps/mobile/.expo | awk '{print "Total: " $1}' >> $GITHUB_STEP_SUMMARY
          fi

  # Comment on PR with results
  pr-comment:
    name: PR Comment
    runs-on: ubuntu-latest
    needs: [validate, quick-check, size-check]
    if: always()
    permissions:
      pull-requests: write
    steps:
      - name: Create PR comment
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const results = {
              validate: '${{ needs.validate.result }}',
              quickCheck: '${{ needs.quick-check.result }}',
              sizeCheck: '${{ needs.size-check.result }}'
            };

            const emoji = (result) => {
              if (result === 'success') return '✅';
              if (result === 'failure') return '❌';
              if (result === 'skipped') return '⏭️';
              return '⚠️';
            };

            const body = `## PR Check Results

            ${emoji(results.validate)} **Validation**: ${results.validate}
            ${emoji(results.quickCheck)} **Quick Check**: ${results.quickCheck}
            ${emoji(results.sizeCheck)} **Size Check**: ${results.sizeCheck}

            Full CI results available in the [CI workflow](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
            `;

            // Find existing comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' &&
              comment.body.includes('PR Check Results')
            );

            if (botComment) {
              // Update existing comment
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              // Create new comment
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }
