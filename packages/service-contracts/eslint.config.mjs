import nodeConfig from "@app/eslint-config/node.js";

/**
 * ESLint configuration for the service-contracts package
 * Uses shared Node.js config from packages/eslint-config
 */
export default nodeConfig;
